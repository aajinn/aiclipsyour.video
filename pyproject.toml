[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-v --tb=short"

[tool.hypothesis]
# Global Hypothesis settings applied to all property tests
max_examples = 100
suppress_health_check = ["too_slow"]
